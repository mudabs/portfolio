Steps to fix(Alma Linux)

SSH into your VPS:

ssh root@74.208.155.239
dd6WYp6y

Create the folder structure:

sudo mkdir -p /var/www/pengilonsecurity/dist


Set permissions (optional, but recommended):

sudo chown -R nginx:nginx /var/www/pengilonsecurity
sudo chmod -R 755 /var/www/pengilonsecurity
sestatus
sudo chcon -R -t httpd_sys_content_t /var/www/pengilonsecurity
sudo semanage fcontext -a -t httpd_sys_content_t "/var/www/pengilonsecurity(/.*)?"
sudo restorecon -Rv /var/www/pengilonsecurity



Exit the VPS and copy your files from your local machine:

scp -r dist/* root@74.208.155.239:/var/www/pengilonsecurity/dist/


Reload Nginx on the server:

sudo nginx -t
sudo systemctl reload nginx


sudo nano /etc/nginx/conf.d/pengilonsecurity.co.zw.conf



Redeployment:

Step 1: Rebuild your app locally
npm run build


This creates a fresh dist/ folder with your production-ready files.

Step 2: Copy files to your VPS

scp -r dist/* root@74.208.155.239:/var/www/pengilonsecurity/